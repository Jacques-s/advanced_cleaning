enum Status { active, inactive }

enum UserRole { admin, siteManager, manager }

enum InspectionFrequency { daily, weekly, monthly, quarterly, annually }

enum InspectionResult { pass, fail, notSet }
